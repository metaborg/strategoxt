include @STRATEGORTS@/share/srts/Makefile.srts

SUFFIXES = .str .cr

SC = @prefix@/bin/sc

.str.c : 
	$(SC) $(STRINCLUDES) $(SCFLAGS) -i $< -o $@ -c

.cr.c : 
	$(SC) $(STRINCLUDES) $(SCFLAGS) -i $< -o $@ -c

%.c.dep : 
	touch $@

CLEANFILES = $(wildcard *.dep)

bootclean:
	rm -f $(BOOTCLEANFILES)
