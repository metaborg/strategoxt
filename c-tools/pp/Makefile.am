include $(top_srcdir)/Makefile.xt
include $(wildcard *.dep)

LANGUAGE=C

pkgdata_DATA = $(LANGUAGE).pp.af $(LANGUAGE).pp
EXTRA_DIST   = $(LANGUAGE).pp.af $(LANGUAGE).pp 
CLEANFILES   = $(LANGUAGE).pp.af

