AC_INIT([concrete-syntax],[0.10],[visser@acm.org])
AM_INIT_AUTOMAKE

USE_XT_PACKAGES

AC_CHECK_PROGS(RPMBUILD, rpmbuild rpm)
AC_SUBST(RPMBUILD)

AC_PROG_CC
AC_PROG_INSTALL

SET_SVN_REVISION
AC_SUBST(SVN_REVISION)

AC_CONFIG_FILES([
  Makefile 
  xtc/Makefile
])
AC_OUTPUT
