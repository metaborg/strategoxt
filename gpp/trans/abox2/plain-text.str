module plain-text

strategies

  // :: _ -> FILE
  topdown-print-to :
    strings -> FILE(name)
      where xtc-new-file => name
          ; <fopen> (name, "w") => stream
          ; <topdown(try(is-string; where(<fputs> (<id>, stream))))> strings
          ; <fclose> stream
