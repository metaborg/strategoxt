module plain-text

strategies

  // :: _ -> FILE
  topdown-print-to :
    strings -> FILE(name)
      where xtc-new-file => name
          ; <open-file> (name, "w")
          ; <topdown(try(is-string; where(<print> (name, [<id>]))))> strings
          ; <close-file> name
