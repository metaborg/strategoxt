#! /bin/sh ../utrecht/rho-run

#include "stdlib.rh"

sum = foldr (x -> y -> x + y) 0;

fromTo = start -> end -> 
  if (start > end) [] (start : (fromTo (start+1) end));

main = print (sum (fromTo 1 10000));
