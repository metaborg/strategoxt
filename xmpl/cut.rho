#! /bin/sh ../utrecht/rho-run

#include "stdlib.rh"

data C;
C :: Int -> C;

f = (C x -> x) (^(C 1000));

g = ((^^fail <+ 200) <+ 300) <+ 100;

h = ((^fail <+ 20) <+ 10) <+ 30;

i = ((fail <+ ^fail) <+ 2) <+ 1;

j = (^^^5000) + (^^^^^^^^5000);

k = ((C 0 -> 300000) (^fail) <+ 100000) <+ 200000;

l = ((^fail) 123 <+ 1000000) <+ 2000000;

main = print ((((((f + g) + h) + i) + j) + k) + l);
