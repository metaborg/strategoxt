module visamb
imports Vis-amb xtc-lib lib

strategies

  visamb = 
    xtc-io-wrap(
      xtc-io-transform(visamb)
    ; xtc-transform(!"ast2abox", build-pp-tables)
    ; xtc-transform(!"abox2text")
    )

  build-pp-tables = 
    !["-p", <xtc-find> "visamb.pp", "-p", <xtc-find> "sdf.cons.pp"]

