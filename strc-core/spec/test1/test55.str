/**
 * This used to be a 'bug', but was not reproducable after some time.
 * https://mail.cs.uu.nl/pipermail/stratego-bugs/2003-December/000215.html
 */
module test55
imports dynamic-rules
strategies

   main =
     not(override rules(Foo: (1, x) -> 3))
/*
 * the not is just because the override should fail (no previous `rules')
 * the real test here is whether it compiles
 */