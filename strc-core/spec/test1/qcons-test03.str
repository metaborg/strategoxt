/**
 * Quoted constructors in a rewrite rule.
 */
module qconc-test03
imports list-cons
strategies

  main =
    !"aap"(1)
    ; do-aap
    ; ?"aap"(2)

rules

  do-aap :
    "aap"(1) -> "aap"(2)

signature
  constructors
   "aap" : Term -> Term
