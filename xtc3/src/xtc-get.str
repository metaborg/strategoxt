/**
 * Transformation Tool Composition (XTC) Command (Get)
 *  
 * @author Niels Janssen (njanssen@cs.uu.nl)
 */
module xtc-get

overlays 
  
strategies

  xtc-get = 
    ?[self, r , f , "get" | ps]
  ; where( <"-r" + "--repository"> r )
  ; xtc-init(|f)
  ; <xtc-xtc-get> ps
 
  xtc-get = 
    ?[self, "get" | ps]
  ; <xtc-xtc-get> ps

  xtc-xtc-get = 
    ?ps
  ; <print>(stdout, <map(<concat-strings> [<xtc-find-loc>,"\n"])> ps )
  ; <exit> 0
    