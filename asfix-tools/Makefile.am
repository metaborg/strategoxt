include $(top_srcdir)/Makefile.xt

SUBDIRS           = sig lib src xtc test
BOOTCLEAN_SUBDIRS = $(SUBDIRS)
EXTRA_DIST        = Makefile.xt autoxt.m4 bootstrap
CLEANFILES        = XTC

ACLOCAL_AMFLAGS = -I .
