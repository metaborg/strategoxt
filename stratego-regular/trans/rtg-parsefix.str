/**
 * Removes quotes and escapes in strings of quoted (non) terminals.
 *
 * @author Martin Bravenboer
 */
module rtg-parsefix
imports liblib rhg-parsefix

strategies

  main-rtg-parsefix =
    io-wrap(tg-parsefix)
