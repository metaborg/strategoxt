include $(top_srcdir)/Makefile.xt

SUBDIRS           = syn sig pp trans lib xtc tests
BOOTCLEAN_SUBDIRS = $(SUBDIRS)
DIST_SUBDIRS      = $(SUBDIRS)

EXTRA_DIST = Makefile.xt  svn-revision

ACLOCAL_AMFLAGS = -I ../config
