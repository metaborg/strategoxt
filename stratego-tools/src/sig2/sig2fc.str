module sig2fc
imports sig2-lib

strategies

  io-sig2fc = str-tg-str-wrap(!"tg2fc")

/*
Usage:
  sig2fc [-h] [-i input] [-o output]

Options:
  -h         Displays usage information.
  -i input   Specificies the Stratego module containg a signature (required).
  -o output  Specificies a file to output the resulting format checker.
  -I dir     Include modules from directory dir.

Description:
   The Format-Checker Generator translates a Stratego signature to a
   Format-Checker for this signature, written in Stratego.
*/
