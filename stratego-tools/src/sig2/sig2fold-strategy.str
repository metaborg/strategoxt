module sig2fold-strategy
imports sig2-lib xtc-lib options

strategies

  io-sig2fold-strategy = str-tg-str-wrap(!"tg2fold-strategy")

/*
Usage:
  sig2fold-strategy [-h] [-i input] [-o output]

Options:
  -h         Displays usage information.
  -i input   Specificies the Stratego module containg a signature (required).
  -o output  Specificies a file to output the resulting fold-strategy module.
  -I dir     Include modules from directory dir.

Description:
   The fold strategy generator translates a Stratego signature to a
   Stratego module containing a fold strategy to systematically 
   translate other languages into the language specified by the
   input signature.
*/

