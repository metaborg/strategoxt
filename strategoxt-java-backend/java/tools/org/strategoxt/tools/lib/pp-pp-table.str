module pp-pp-table
imports 
  libstratego-lib
  libstratego-xtc
  libstratego-gpp

strategies

  main-pp-pp-table =
    xtc-io-wrap(
      pp-pp-table
    )
  
  pp-pp-table =
    if [is-string, is-list => l] then // HACK: allow calling with <call>
      ![l]
    end
  ; ast2abox(|[<import-term(pp-table-pretty.pp.af)>])
  ; box2text-string(|80)

