rm -f config.cache
rm -f acconfig.h

touch README
touch NEWS

export WANT_AUTOMAKE="1.8"
export WANT_AUTOCONF="2.59"

libtoolize || glibtoolize

autoxt
aclocal -I .
autoconf
#acconfig
#autoheader
automake -a

