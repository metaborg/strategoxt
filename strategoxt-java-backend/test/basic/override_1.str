module override_1

imports
  libstratego-lib

rules

  main =
    <string-tokenize(|['*'])> "***"
    
  override string-tokenize(s) = // used by string-tokenize(|t)
    string-tokenize-keep-all(|<s>)

