module internal_1

imports
  libstratego-lib

rules

  main =
    not(<collect-all('*')> "***" => "***")
  ; not(<collect-all('*', id)> ['*','*','*'])
    
  internal collect-all(s,un) =
    string-as-chars(filter(s))

