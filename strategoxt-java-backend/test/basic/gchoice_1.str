module gchoice_1

imports
  libstratego-lib

signature constructors
  Nil  : Nil
  Cons : Term -> Cons

rules

  main =
    not(
      id
      < (id < fail + !3)
      + !2
    )
  ; (id < id + ?never)
  ; not(!never)
