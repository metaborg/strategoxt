module extend_1

imports
  libstratego-lib

rules

  main =
    <collect-all('*')> "***" => "***"
  ; <collect-all('*')> ['*','*','*']
    
  extend collect-all(s,un) =
    string-as-chars(filter(s))

