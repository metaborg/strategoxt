<?xml version="1.0"?>
<project name="org.strategoxt.StrategoXT" basedir="." default="all">
	<!-- Import Ant contrib -->
	<taskdef resource="net/sf/antcontrib/antlib.xml" />
	<!-- Import Stratego/XT Ant contributions -->
	<!--<import file="ant-contrib/strategoxt-contrib.xml" />-->

	<!-- Import the properties file, containing e.g. the version number -->
	<property file="build.properties" />

	<!-- Import syntax builder -->
	<!--<include file="syntax/build.xml" as="syntax" />-->

	<target name="all" depends="
		create-dirs,
		syntax
		" />
	<target name="create-dirs">
		<mkdir dir="dist" />
	</target>

	<target name="clean">
		<delete dir="dist" />
		<!-- Clean the syntax -->
		<ant dir="syntax" antfile="build.xml" target="clean" />
	</target>

	<target name="syntax">
		<!-- Build the syntax -->
		<!--<antcall target="syntax.all" />-->
		<ant dir="syntax" antfile="build.xml" target="all" />
	</target>
</project>