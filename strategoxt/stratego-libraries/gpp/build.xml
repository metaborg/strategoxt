<?xml version="1.0"?>
<project name="libstratego-gpp" default="all">
	<!-- Import Stratego/XT Ant contributions -->

	<!-- use this is for a build with a strategoxt.jar that does not include the Ant contributions -->
	<import file="../../ant-contrib/strategoxt-antlib.xml" />

	<!-- use this for a build with recent strategoxt.jar that has the Ant contributions inside -->
	<!-- <typedef resource="strategoxt-antlib.xml" /> -->

	<!-- Import Ant contrib -->
	<taskdef resource="net/sf/antcontrib/antlib.xml" />

	<!-- Build configuration. Overwrite these from importer -->
	<property name="sdf2bundle" value="${user.home}/.nix-profile" />
	<property name="nativepath" value="${sdf2bundle}/bin/" />
	<property name="install-prefix" location="dist" />
	<property name="install-location" location="${install-prefix}/share/strategoxt/libstratego-gpp" />

	<property name="dep.aterm-front" location="../../syntax/aterm-front/dist/share/strategoxt/aterm-front" />
	<property name="dep.gpp" location="../../syntax/gpp/dist/share/strategoxt/gpp" />

	<property name="syn" value="syn" />
	<property name="bin" value="bin" />

	<property name="bin.stratego" value="${bin}/stratego" />
	<property name="bin.java" value="${bin}/java" />

	<target name="all" depends="build, install" />
	<target name="build" depends="build-stratego, build-java" />

	<target name="build-stratego">
		<mkdir dir="${bin.stratego}/org/strategoxt/stratego_gpp" />
		<strj-lib input="lib/stratego-gpp.str" output="${bin.stratego}/org/strategoxt/stratego_gpp/Main.java" package="org.strategoxt.stratego_gpp">
			<strjlibargs>
				<arg value="-la" />
				<arg value="stratego-lib" />
				<arg value="-la" />
				<arg value="stratego-sglr" />
				<arg value="-la" />
				<arg value="org.strategoxt.lang.compat.override.gpp_performance" />
				<arg value="-I" />
				<arg value="${dep.gpp}/syn/box" />
				<arg value="-I" />
				<arg value="${dep.gpp}/syn/pp-table" />
			</strjlibargs>
			<str-deps>
				<srcfiles dir="lib" includes="**/*.str" />
				<srcfiles dir="${dep.gpp}/syn/box" includes="**/*" />
				<srcfiles dir="${dep.gpp}/syn/pp-table" includes="**/*" />
			</str-deps>
		</strj-lib>
		<!-- <copy file="lib/libstratego-gpp.ctree" todir="${bin}" /> -->
		<copy file="lib/libstratego-gpp.rtree" todir="${bin}" />
	</target>

	<target name="build-java" depends="build-stratego">
		<mkdir dir="${bin.java}" />
		<javac srcdir="${bin.stratego}" destdir="${bin.java}" classpath="${bin.stratego}" source="1.5" target="1.5" nowarn="off" debug="true" debuglevel="lines,vars,source" />
	</target>

	<target name="jar" depends="build-java">
		<jar destfile="${bin}/libstratego-gpp.jar" update="false">
			<fileset dir="${bin.java}" includes="**/*.class" />
			<fileset dir="${bin.stratego}" includes="**/*.rtree, **/*.pp.af, **/*.tbl" />
		</jar>
	</target>

	<target name="install" depends="jar">
		<mkdir dir="${install-location}" />
		<copy todir="${install-location}">
			<fileset dir="${bin}" includes="*.rtree, *.jar" />
		</copy>
	</target>

	<target name="clean">
		<delete dir="${bin}" />
		<delete dir="${install-location}" />
		<delete>
			<fileset dir="lib" includes="**/*.ctree*, **/*.rtree" />
		</delete>
	</target>
</project>