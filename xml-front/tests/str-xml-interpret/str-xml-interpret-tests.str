module str-xm-interpret-tests
imports interpret-xml
        sunit

strategies

  main-str-xml-interpret-tests =
    test-suite(!"Interpreted xml in Stratego", 
      apply-test(!"Simple exp0 build", exp0-xml, !(), exp0-aterm)
    ; apply-test(!"Simple exp1 build", exp1-xml, !(), exp1-aterm)
    ; apply-test(!"Simple exp2 build", exp2-xml, !(), exp2-aterm)
    ; apply-test(!"Simple exp3 build", exp3-xml, !(), exp3-aterm)
    ; apply-test(!"Simple exp4 build", exp4-xml, !(), exp4-aterm)

    // ; apply-test(!"exp3", exp3-xml, exp3-aterm)
    // ; apply-test(!"exp4", exp4-xml, exp4-aterm)
    )